version: '3.8'

services:
  fde-dda-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fde-dda-service
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount app directory for hot reload during development
      - ./app:/app/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - dda-network

networks:
  dda-network:
    driver: bridge

# Note: Redis will be added in Phase 2 for distributed caching
# volumes:
#   redis-data:
#
# services:
#   redis:
#     image: redis:7-alpine
#     container_name: fde-dda-redis
#     ports:
#       - "6379:6379"
#     volumes:
#       - redis-data:/data
#     networks:
#       - dda-network
#     command: redis-server --appendonly yes
