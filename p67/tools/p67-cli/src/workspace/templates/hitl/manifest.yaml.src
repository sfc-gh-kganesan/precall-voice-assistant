name: hitl_workflow
visibility: public

params:
    action:
        description: Action or change to request human approval for

config:
    - config_name: default
      parameters:
          action:
              value: "Deploy the latest model to production"
