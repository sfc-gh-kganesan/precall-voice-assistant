COPY INTO {{ KB_DATABASE_NAME }}.{{ KB_SCHEMA_NAME }}.KB_KNOWLEDGE
FROM @{{ KB_DATABASE_NAME }}.{{ KB_SCHEMA_NAME }}.KNOWLEDGE
FILE_FORMAT = {{ KB_DATABASE_NAME }}.{{ KB_SCHEMA_NAME }}.KB_KNOWLEDGE_CSV
ON_ERROR = CONTINUE;
