CREATE TABLE IF NOT EXISTS {{ KB_DATABASE_NAME }}.{{ KB_SCHEMA_NAME }}.EVALUATION_RESULTS (
    SEARCH_ID NUMBER(38, 0),
    INPUT_QUERY VARCHAR(16777216),
    EVALUATION_MODEL VARCHAR(16777216),
    EVALUATION VARIANT,
    CREATED_BY VARCHAR(16777216),
    CREATED_ON TIMESTAMP_NTZ(9)
);

ALTER TABLE {{ KB_DATABASE_NAME }}.{{ KB_SCHEMA_NAME }}.EVALUATION_RESULTS
ADD CONSTRAINT FK_EVALUATION_RESULTS_SEARCH_ID
FOREIGN KEY (SEARCH_ID) REFERENCES {{ KB_DATABASE_NAME }}.{{ KB_SCHEMA_NAME }}.SEARCH_QUERIES (SEARCH_ID);
