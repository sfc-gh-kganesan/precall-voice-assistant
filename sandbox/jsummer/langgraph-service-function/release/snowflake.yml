definition_version: 2
entities:
  compute_pool:
    type: compute-pool
    identifier:
      name: sandbox_compute_pool_cpu
    min_nodes: 1
    max_nodes: 1
    instance_family: CPU_X64_XS
    auto_resume: true
    initially_suspended: true
    auto_suspend_seconds: 60
  image_repository:
    type: image-repository
    identifier: 
      name: image_repository
      database: JSUMMER
      schema: SANDBOX
  langgraph:
    type: service
    identifier: 
      name: langgraph
      database: JSUMMER
      schema: SANDBOX
    stage: DROPBOX
    compute_pool: sandbox_compute_pool_cpu
    spec_file: service_spec.yaml
    min_instances: 1
    max_instances: 1
    auto_resume: true
    artifacts:
      - service_spec.yaml
    comment: "LangGraph service"