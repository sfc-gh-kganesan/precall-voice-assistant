[project]
name = "pattern-distiller"
version = "0.1.0"
description = "Extract interaction patterns (Recipes) from high-utility code files"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]
