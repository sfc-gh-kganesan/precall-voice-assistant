[project]
name = "pattern-architect"
version = "0.1.0"
description = "Scan project structure, group files into logical domains, assign utility scores"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]
